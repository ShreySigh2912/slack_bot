services:
  - type: web
    name: admission-bot
    env: node
    buildCommand: npm install
    startCommand: node app.js
    envVars:
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
    plan: free
    region: oregon
    autoDeploy: true
